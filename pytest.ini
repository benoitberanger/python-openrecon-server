[pytest]
markers =
    integration: end-to-end tests exercising real sockets and/or real MRD
        sample files (slower, run separately from the fast unit suite).

# Fast default run for local dev / CI pre-merge:
#   pytest -m "not integration"
# Full run :
#   pytest -rs